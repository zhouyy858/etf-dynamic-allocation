---
name: etf-dynamic-allocation
description: 五只ETF(159232自由现金流/515100红利低波100/159941纳指100/513500标普500/159952创业板)的长期动态配置策略：每周三调仓、每次目标分3周三笔(每周三1/3)完成；15%国内+15%海外固定底仓；现金全部做国债逆回购；含可复现回测框架、静态基准对比、压力测试与投资执行手册。当用户要求对这五只ETF做动态组合回测、每周三调仓方案、牛熊切换/仓位管理、压力测试、或生成可执行投资手册时使用。
metadata:
  short-description: 五只ETF每周三动态调仓回测与执行手册
---

# ETF 动态配置策略（v10 定稿）

## 概述

双市场（A股/美股）趋势打分驱动的动态配置，标的固定为 159232/515100/159941/513500/159952。
最终结果（v10，2026-07-31 数据）：全历史 2014-2026 CAGR 12.20% / MDD -14.89% / Sharpe 1.08；真实 ETF 牛市窗口 25.24% / -6.44% / 3.92。
完整参数与结论见 `references/investment_manual.md`、`references/final_cfg_v10.json`。

## 硬性业务约束（回测与执行必须遵守，不得简化）

1. 数据：每只 ETF 用最长可用日度净值（复权）；159232→自由现金流指数(932365)、515100→红利低波100指数(930955)、159941→纳指100、513500→标普500、159952→创业板指 为代理口径。
2. 调仓：**每周三执行调仓；每次目标缺口分 3 笔、在决策当周及随后 2 个周三各成交 1/3，三周调整完**；计划不重叠（上一计划未完成则常规决策顺延）；紧急风控任意交易日可触发并取代旧计划，但仍分 3 周三笔。
3. 底仓：国内 159232+515100 各 7.5%、海外 159941+513500 各 7.5% 固定不动（合计 30%），永不清零。
4. 现金：闲置现金全部配置国债逆回购（回测按 1.8% 年化日计息）。
5. 费用：单边万 5（佣金+滑点合并），逐笔扣除；min_delta=0.2% 过滤噪声调仓。
6. QDII 现实风险：溢价率 >2% 停买、>5% 用申赎替代；净值滞后一日；涨跌停封板顺延（回测未建模，手册 §10 有对策）。
7. 输出：**只允许终端文本/ASCII 图表（python 实时绘制），禁止生成图片文件**。

## 工作流（每轮固定闭环，禁止单次输出直接结束）

1. **资料获取**：确认数据最新日（`assets/data/panel_proxy_rets.csv` / `panel_real_rets.csv`）；数据过期时运行 `scripts/fetch_*.py` 刷新。
2. **数据分析**：统计各标的收益率/波动率/最大回撤/相关性/滚动夏普；校验跨市场相关性（A股/美股）与极端行情传导。
3. **组合构建**：默认使用 `references/final_cfg_v10.json`；如需优化用 `scripts/search.py` 做参数搜索（评分：全历史 Calmar 优先，约束真实窗口 CAGR>=20% 且 MDD>=-10%）。
4. **历史回测**：`scripts/run_backtest.py`（周三决策→三周三笔成交、无未来函数、费用与逆回购计入）；输出 DYN 与 B1-B7 静态基准对比 + 分阶段表现 + 仓位现状 + 终端图表。
5. **压力测试**：`scripts/stress_test.py` 三情景（牛市/震荡/熊市）+ 跨市场相关性校验。
6. **严格自我评审**：逐条批判收益漏洞、最大回撤隐患、风格冲突、调仓缺陷、假设风险；生成待优化清单。
7. **迭代改进**：修改参数/规则后重跑 2-5，把结果追加到 `iterations` 进展；只有达标（牛市窗口 20%+ 且回撤<10%；全历史说明真实边界）才终止。

## 脚本用法

```bash
PY=python3   # 需 pandas/numpy；如用 Codex runtime: /Users/mac/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3
cd <你的工作目录>
$PY /path/to/skill/scripts/run_backtest.py <cfg.json或默认v10> <tag>
$PY /path/to/skill/scripts/stress_test.py
$PY /path/to/skill/scripts/search.py 120 23
```

- 脚本在 skill 内互相 import（`sys.path` 已处理）；输出写到当前工作目录的 `out/`。
- `run_backtest.py` 输出：DYN+基准指标表、分阶段表、**当前仓位现状**（实际 vs 目标权重、信号打分、CN/US 市场回撤与锁定状态）、终端 ASCII 净值/回撤/迭代进展图。
- 刷新数据：`fetch_data.py`（ETF 净值，东方财富）、`fetch_tencent.py`（指数，腾讯）、`fetch_ext.py`（中证指数/QDII 联接）→ 覆盖 `assets/data/` 后重跑回测。

## 策略核心（简述，细节看手册）

- **打分**：沪深300 与创业板指各「收盘>120/60/20日均线」×3 + 纳指与标普「>120日均线」×2 + 纳指「>60日均线」×1 = 0-9 分；向上平滑 0.58、向下 0.18。
- **状态骨架**：`state_map`（v10）9 分→成长86/防御4 … 0 分→成长11/防御30；状态 3-9 用成长拆分 159952:159941:513500 = 36:48:20，状态 0-2 用 44:36:20。
- **风控层级**：结构闸门（破 120 日线→该市场成长弹性×0）；快刹车（CN 回撤>7%/US>10% 且破 SMA20→成长仓×0.08/×0.12）；深熊锁定（CN 回撤>22%/US>24%→该市场弹性全清只留 15% 底仓）；组合回撤上限（-12%/-18%/-25%→权益 80%/65%/50%，打分<6 时）；波动率目标 18%。
- **回撤口径**：市场回撤用 3 年滚动窗口（755 交易日）峰值，禁止用全历史峰值（会导致 CN 成长仓永久锁死）。
- **验收边界（诚实）**：全历史（含三轮大熊）无法同时达到 20% 年化与 <10% 回撤（前沿约 12%/-15%）；牛市情景 25.2%/-6.4% 达标。任何"全周期 20%+10%"宣称都是数据操纵或满仓赌注。

## 参考文件

- `references/investment_manual.md`：完整投资手册（权重表/触发阈值/三周三笔细则/牛熊切换/止盈/逆回购/QDII 对策/压力测试/残余风险）。
- `references/final_cfg_v10.json`：定稿参数（run_backtest 默认加载）。
- `references/iterations.json`：v2→v10 迭代进展；`references/stress_test.json`：三情景压力测试；`references/risk_analysis.json`：相关性/极端窗口。
- `assets/data/`：回测数据面板（代理 2014 起 + 真实 2025-04 起）。
